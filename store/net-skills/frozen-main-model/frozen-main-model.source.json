{
  "neurons": [
    {
      "name": "frozen",
      "value": 0,
      "definition": "the main model can be read while a skill is built, and not written"
    },
    {
      "name": "digest",
      "value": 0,
      "definition": "the fingerprint taken before and compared after, so the freeze is checked not promised"
    },
    {
      "name": "already-true",
      "value": 0,
      "definition": "a requirement the main model already covers, which must not be rebuilt"
    },
    {
      "name": "skill-neurons",
      "value": 0,
      "definition": "the new neurons this skill contributes, held apart from the main model"
    },
    {
      "name": "install-joins-mesh",
      "value": 0,
      "definition": "installing is what wires those neurons all-to-all into the network"
    }
  ],
  "code": "name=\"frozen\"\n\"frozen\"@definishon=\"the main model can be read while a skill is built, and not written\"\nname=\"digest\"\n\"digest\"@definishon=\"the fingerprint taken before and compared after, so the freeze is checked not promised\"\nname=\"already-true\"\n\"already-true\"@definishon=\"a requirement the main model already covers, which must not be rebuilt\"\nname=\"skill-neurons\"\n\"skill-neurons\"@definishon=\"the new neurons this skill contributes, held apart from the main model\"\nname=\"install-joins-mesh\"\n\"install-joins-mesh\"@definishon=\"installing is what wires those neurons all-to-all into the network\"\n\"frozen\"@connections=\".digest\"\n\"digest\"@connections=\".already-true\"\n\"already-true\"@connections=\".skill-neurons\"\n\"skill-neurons\"@connections=\".install-joins-mesh\"\n\"install-joins-mesh\"@connections=\".frozen\""
}