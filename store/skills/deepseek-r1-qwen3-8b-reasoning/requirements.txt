# Enough to load and run the model locally. qwen3 needs a reasonably recent
# transformers; the rest are the usual loading path.
transformers>=4.51
torch>=2.4
accelerate>=0.34
safetensors>=0.4
huggingface_hub>=0.26
# Optional: 4-bit loading, which is what makes this fit on an 8GB card.
bitsandbytes>=0.43 ; platform_system == "Linux"
