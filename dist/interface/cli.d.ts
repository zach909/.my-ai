import type { NeuroclawLLM } from "../models && skills/llm.js";
import type { NeuroPipeline } from "../models && skills/core/pipeline.js";
import type { ThesaurusDictionary } from "../models && skills/thesaurus.js";
import type { PluginRegistry } from "../plugin_manager/registry.js";
import type { SystemAccess } from "./system-access.js";
import type { MultiDesktopManager } from "./multi-desktop.js";
export declare class CLI {
    private llm;
    private pipeline;
    private thesaurus;
    private pluginRegistry;
    private systemAccess?;
    private multiDesktop?;
    private rl;
    private interactiveMode;
    private commandQueue;
    private queueRunning;
    private neuriBlockMode;
    private neuriBlockLines;
    constructor(llm: NeuroclawLLM, pipeline: NeuroPipeline, thesaurus: ThesaurusDictionary, pluginRegistry: PluginRegistry, systemAccess?: SystemAccess, multiDesktop?: MultiDesktopManager);
    private enqueue;
    private drainQueue;
    private colorize;
    private printLogo;
    private printWelcome;
    startInteractive(): Promise<void>;
    private printHelp;
    private printStatus;
    private printDesktopStatus;
    private startChat;
    private handleGenerate;
    private handleThink;
    private handleTrain;
    private handleSearch;
    private handleNeuri;
    private handleDict;
    private handleBuild;
    private printPlugins;
    private handleSave;
    private handleQuantize;
}
