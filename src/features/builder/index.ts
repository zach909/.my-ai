/**
 * Extension Builder feature — visual editor over the real engine
 * (extension-builder/builder.js), running entirely in the browser.
 */
export { useBuilder } from './use-builder';
export type { BuilderApi, NeuronData, ConnectionData, LabelData } from './use-builder';
export { BuilderCanvas } from './builder-canvas';
