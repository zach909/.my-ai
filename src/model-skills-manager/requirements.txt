# Mesh AI dependencies. Install torch matching your CUDA version from
# https://pytorch.org/get-started/locally/ if the default wheel doesn't match
# your GPU. For an RTX 5070 (Blackwell / CUDA 12.x) use a recent PyTorch build.
# These three are all the canonical all-to-all mesh needs.
torch>=2.1
numpy>=1.24
sentencepiece>=0.1.99

# OPTIONAL — only for the standalone Section 5.2 quantum component
# (tinygpt/elastic_mesh.py). PennyLane simulates its per-expert interference as
# a genuine small variational quantum circuit (statevector simulation, no real
# QPU needed). The canonical mesh does not need it; test_elastic_mesh.py skips
# cleanly when it is absent.
# pennylane>=0.35

# OPTIONAL — only for desktop_app.py (the native desktop chat window).
# Tkinter is Python's own stdlib GUI toolkit, not a pip package, so it isn't
# listed above: it ships by default with the python.org Windows/macOS
# installers, but on Debian/Ubuntu it's a separate OS package matched to your
# Python's minor version, e.g.:
#   sudo apt install python3.11-tk   # check your version with `python3 --version`
# test_desktop_app.py skips cleanly when tkinter isn't importable.
